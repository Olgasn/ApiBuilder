﻿using MediatR;
using {0};
using {1};
using {2};

namespace {3};

public class Get{4}ByIdQueryHandler(I{4}Repository repository) : IRequestHandler<Get{4}ByIdQuery, {4}?>
{{
	private readonly I{4}Repository _repository = repository;

	public async Task<{4}?> Handle(Get{4}ByIdQuery request, CancellationToken cancellationToken) => 
		await _repository.GetById(request.Id, trackChanges: false);
}}